matrix:
  allow_failures:
    - os: osx
  include:
    - os: linux
      dist: trusty
      language: minimal
      services: docker
      env: DOCKER_IMAGE=ubuntu:19.04
    - os: osx
      language: generic
script:
  - source ./travis/dist_${TRAVIS_OS_NAME}.sh
  - ./dist/weasyprint --version
